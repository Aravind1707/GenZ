# GenZ Gaming Cafe OS

LAN-first gaming cafe management, ordering, booking, billing, kitchen, membership and finance system.

## Development

```bash
npm install
npm run dev
```

The first release uses a local demo data layer so the UI can be developed and tested before wiring the production database and existing membership system.
