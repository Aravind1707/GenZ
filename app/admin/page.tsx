'use client';
import {useEffect,useState} from 'react';
type Data={menu:any[];rates:any[];stations:any[];packages:any[]};
export default function Admin(){const[data,setData]=useState<Data>({menu:[],rates:[],stations:[],packages:[]});const[staff,setStaff]=useState<any[]>([]);const[error,setError]=useState('');const[form,setForm]=useState<any>({name:'',category:'FOOD',normalPrice:'',memberPrice:'',imageUrl:'',stockQty:'0'});const[rate,setRate]=useState<any>({stationType:'PC',pcTier:'NORMAL',label:'',regularPrice:'',memberPrice:'',unitLabel:'per hour',specs:'',imageUrl:''});const[pkg,setPkg]=useState<any>({stationGroup:'PC240_MULTI',label:'240Hz PC / PS5 Multi',durationMinutes:'60',regularPrice:'120',memberPrice:'90',extraPlayerSurcharge:'0',sortOrder:'10'});const[station,setStation]=useState<any>({name:'',type:'PC',pcTier:'NORMAL',status:'AVAILABLE',hourlyRate:'',slotMinutes:'60'});const[staffForm,setStaffForm]=useState<any>({username:'',name:'',role:'MANAGER',password:''});
async function load(){const[r,s]=await Promise.all([fetch('/api/admin/catalog',{cache:'no-store'}),fetch('/api/admin/staff',{cache:'no-store'})]);const rd=await r.json(),sd=await s.json();if(!r.ok)throw Error(rd.error||'Admin access required');if(!s.ok)throw Error(sd.error||'Unable to load staff');setData(rd);setStaff(sd.staff)}useEffect(()=>{load().catch(e=>setError(e.message))},[]);async function post(url:string,body:any){setError('');try{const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)throw Error(d.error||'Update failed');await load()}catch(e){setError(e instanceof Error?e.message:'Update failed')}}async function toggleStaff(s:any){await post('/api/admin/staff',{action:'update',id:s.id,active:!s.active})}async function toggleMenu(m:any){await post('/api/admin/catalog',{action:'menu-status',id:m.id,active:!m.active})}async function saveMenu(m:any){await post('/api/admin/catalog',{action:'menu',id:m.id,name:m.name,category:m.category,imageUrl:m.image_url,normalPrice:Number(m.non_member_price),memberPrice:Number(m.member_price),stockQty:Number(m.stock_qty)})}
return <main><div className="top"><div><div className="eyebrow">OWNER / CONFIGURATION</div><div className="title">Administration</div><div className="muted">Catalogue, gaming rates, membership price packages, stations and staff lifecycle controls. All mutations are owner-only and audited.</div></div></div>{error&&<div className="card danger" style={{marginBottom:16}}>{error}</div>}
<section className="card"><div className="sectionTitle">Developer Console</div><p className="muted">Create a dedicated developer account for engineering access to feature switches and audit logs. Developers cannot use owner-only administration permissions.</p><a className="btn primary" href="/developer">Open Developer Console</a></section>
<section className="card" style={{marginTop:16}}><div className="sectionTitle">Membership</div><p className="muted">GenZ Gaming Cafe uses one membership type with no tiers. Membership IDs use the GENZFAM### format and require name, mobile number and government ID proof.</p><a className="btn primary" href="/members">Open member management</a></section>
<section className="card" style={{marginTop:16}}><div className="sectionTitle">Membership price packages</div><div className="actions" style={{marginTop:12,flexWrap:'wrap'}}><input placeholder="Group" value={pkg.stationGroup} onChange={e=>setPkg({...pkg,stationGroup:e.target.value})}/><input placeholder="Label" value={pkg.label} onChange={e=>setPkg({...pkg,label:e.target.value})}/><input type="number" placeholder="Duration min" value={pkg.durationMinutes} onChange={e=>setPkg({...pkg,durationMinutes:e.target.value})}/><input type="number" placeholder="Regular ₹" value={pkg.regularPrice} onChange={e=>setPkg({...pkg,regularPrice:e.target.value})}/><input type="number" placeholder="Member ₹" value={pkg.memberPrice} onChange={e=>setPkg({...pkg,memberPrice:e.target.value})}/><input type="number" placeholder="Extra player ₹" value={pkg.extraPlayerSurcharge} onChange={e=>setPkg({...pkg,extraPlayerSurcharge:e.target.value})}/><input type="number" placeholder="Sort" value={pkg.sortOrder} onChange={e=>setPkg({...pkg,sortOrder:e.target.value})}/><button className="btn primary" onClick={()=>post('/api/admin/catalog',{action:'package',...pkg,durationMinutes:Number(pkg.durationMinutes),regularPrice:Number(pkg.regularPrice),memberPrice:Number(pkg.memberPrice),extraPlayerSurcharge:Number(pkg.extraPlayerSurcharge),sortOrder:Number(pkg.sortOrder)})}>Create / save package</button></div><div className="tableWrap" style={{marginTop:16}}><table><thead><tr><th>Group</th><th>Label</th><th>Duration</th><th>Regular</th><th>Member</th><th>Extra</th></tr></thead><tbody>{data.packages.map(p=><tr key={p.id}><td>{p.station_group}</td><td>{p.label}</td><td>{p.duration_minutes} min</td><td>₹{p.regular_price}</td><td>₹{p.member_price}</td><td>{p.extra_player_surcharge?`₹${p.extra_player_surcharge}`:'—'}</td></tr>)}</tbody></table></div></section>
<section className="card" style={{marginTop:16}}><div className="sectionTitle">Menu catalogue</div><div className="actions" style={{marginTop:12,flexWrap:'wrap'}}><input placeholder="Name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Category" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/><input type="number" placeholder="Normal ₹" value={form.normalPrice} onChange={e=>setForm({...form,normalPrice:e.target.value})}/><input type="number" placeholder="Member ₹" value={form.memberPrice} onChange={e=>setForm({...form,memberPrice:e.target.value})}/><input type="number" placeholder="Stock" value={form.stockQty} onChange={e=>setForm({...form,stockQty:e.target.value})}/><input placeholder="Image URL" value={form.imageUrl} onChange={e=>setForm({...form,imageUrl:e.target.value})}/><button className="btn primary" onClick={()=>post('/api/admin/catalog',{action:'menu',...form,normalPrice:Number(form.normalPrice),memberPrice:Number(form.memberPrice),stockQty:Number(form.stockQty)})}>Create menu item</button></div><table className="table" style={{marginTop:16}}><thead><tr><th>Name</th><th>Normal</th><th>Member</th><th>Stock</th><th>Status</th><th>Action</th></tr></thead><tbody>{data.menu.map(m=><tr key={m.id}><td><input value={m.name} onChange={e=>setData({...data,menu:data.menu.map(x=>x.id===m.id?{...x,name:e.target.value}:x)})}/><div className="muted">{m.category}</div></td><td><input type="number" value={m.non_member_price} onChange={e=>setData({...data,menu:data.menu.map(x=>x.id===m.id?{...x,non_member_price:e.target.value}:x)})}/></td><td><input type="number" value={m.member_price} onChange={e=>setData({...data,menu:data.menu.map(x=>x.id===m.id?{...x,member_price:e.target.value}:x)})}/></td><td>{m.stock_qty}</td><td>{m.active?'ACTIVE':'DISABLED'}</td><td><button className="btn" onClick={()=>saveMenu(m)}>Save</button> <button className="btn" onClick={()=>toggleMenu(m)}>{m.active?'Disable':'Enable'}</button></td></tr>)}</tbody></table></section>
<section className="card" style={{marginTop:16}}><div className="sectionTitle">Gaming rates</div><div className="actions" style={{marginTop:12,flexWrap:'wrap'}}><select value={rate.stationType} onChange={e=>setRate({...rate,stationType:e.target.value})}>{['PC','PS5','PS4','PSVR','MOZA'].map(x=><option key={x}>{x}</option>)}</select><select value={rate.pcTier} onChange={e=>setRate({...rate,pcTier:e.target.value})}><option>NORMAL</option><option>PREMIUM</option></select><input placeholder="Label" value={rate.label} onChange={e=>setRate({...rate,label:e.target.value})}/><input type="number" placeholder="Regular ₹" value={rate.regularPrice} onChange={e=>setRate({...rate,regularPrice:e.target.value})}/><input type="number" placeholder="Member ₹" value={rate.memberPrice} onChange={e=>setRate({...rate,memberPrice:e.target.value})}/><input placeholder="Unit" value={rate.unitLabel} onChange={e=>setRate({...rate,unitLabel:e.target.value})}/><input placeholder="Specs" value={rate.specs} onChange={e=>setRate({...rate,specs:e.target.value})}/><button className="btn primary" onClick={()=>post('/api/admin/catalog',{action:'rate',...rate,regularPrice:Number(rate.regularPrice),memberPrice:Number(rate.memberPrice)})}>Create / save rate</button></div><table className="table" style={{marginTop:16}}><thead><tr><th>Type</th><th>Label</th><th>Regular</th><th>Member</th><th>Status</th></tr></thead><tbody>{data.rates.map(r=><tr key={r.id}><td>{r.station_type}</td><td>{r.label}</td><td>{r.regular_price}</td><td>{r.member_price}</td><td>{r.active?'ACTIVE':'DISABLED'}</td></tr>)}</tbody></table></section>
<section className="card" style={{marginTop:16}}><div className="sectionTitle">Stations</div><div className="actions" style={{marginTop:12,flexWrap:'wrap'}}><input placeholder="Station name" value={station.name} onChange={e=>setStation({...station,name:e.target.value})}/><select value={station.type} onChange={e=>setStation({...station,type:e.target.value})}>{['PC','PS5','PS4','PSVR','MOZA'].map(x=><option key={x}>{x}</option>)}</select><select value={station.pcTier} onChange={e=>setStation({...station,pcTier:e.target.value})}><option>NORMAL</option><option>PREMIUM</option></select><select value={station.status} onChange={e=>setStation({...station,status:e.target.value})}><option>AVAILABLE</option><option>BOOKED</option><option>ACTIVE</option><option>MAINTENANCE</option><option>BLOCKED</option></select><input type="number" placeholder="Hourly ₹" value={station.hourlyRate} onChange={e=>setStation({...station,hourlyRate:e.target.value})}/><input type="number" placeholder="Slot minutes" value={station.slotMinutes} onChange={e=>setStation({...station,slotMinutes:e.target.value})}/><button className="btn primary" onClick={()=>post('/api/admin/catalog',{action:'station',...station,hourlyRate:Number(station.hourlyRate),slotMinutes:Number(station.slotMinutes)})}>Create / save station</button></div><table className="table" style={{marginTop:16}}><thead><tr><th>Name</th><th>Type</th><th>Status</th><th>Rate</th></tr></thead><tbody>{data.stations.map(s=><tr key={s.id}><td>{s.name}</td><td>{s.type}</td><td>{s.status}</td><td>{s.hourly_rate}</td></tr>)}</tbody></table></section>
<section className="card" style={{marginTop:16}}><div className="sectionTitle">Staff lifecycle</div><div className="actions" style={{marginTop:12,flexWrap:'wrap'}}><input placeholder="Username" value={staffForm.username} onChange={e=>setStaffForm({...staffForm,username:e.target.value})}/><input placeholder="Name" value={staffForm.name} onChange={e=>setStaffForm({...staffForm,name:e.target.value})}/><select value={staffForm.role} onChange={e=>setStaffForm({...staffForm,role:e.target.value})}><option>MANAGER</option><option>DEVELOPER</option><option>OWNER</option></select><input type="password" placeholder="Password (10+ chars)" value={staffForm.password} onChange={e=>setStaffForm({...staffForm,password:e.target.value})}/><button className="btn primary" onClick={()=>post('/api/admin/staff',{action:'create',...staffForm})}>Create staff</button></div><table className="table" style={{marginTop:16}}><thead><tr><th>User</th><th>Role</th><th>Active</th><th>Action</th></tr></thead><tbody>{staff.map(s=><tr key={s.id}><td>{s.username}<div className="muted">{s.name}</div></td><td>{s.role}</td><td>{s.active?'YES':'NO'}</td><td><button className="btn" onClick={()=>toggleStaff(s)}>{s.active?'Disable':'Enable'}</button></td></tr>)}</tbody></table></section></main>}
