'use client';
import {useState} from 'react';

type Receipt={receiptId:string;sessionId:string;stationId:string;customerName:string;issuedAt:string;gaming:{total:number;participants:Array<{name:string;charge:number;memberTier?:string}>};food:Array<{orderId:string;status:string;paymentMode:string;paymentStatus:string;createdAt:string;total:number;items:Array<{name:string;qty:number;unitPrice:number;participantName?:string}>}>;totals:{gaming:number;food:number;depositApplied:number;groupAllocatedGaming:number;groupAllocatedFood:number;adjustments:number;creditApplied:number;paid:number;outstanding:number};payments:Array<{id:string;method:string;amount:number;status:string;createdAt:string;voidedAt?:string;voidReason?:string}>};
const money=(n:number)=>`₹${Number(n||0).toLocaleString('en-IN')}`;
export default function Receipts(){const[id,setId]=useState(''),[receipt,setReceipt]=useState<Receipt|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const load=async()=>{const sessionId=id.trim();if(!sessionId)return setError('Enter a session ID.');setBusy(true);setError('');try{const r=await fetch(`/api/sessions/receipt?sessionId=${encodeURIComponent(sessionId)}`,{cache:'no-store'}),j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||'Unable to load receipt');setReceipt(j.receipt)}catch(e){setReceipt(null);setError(e instanceof Error?e.message:'Unable to load receipt')}finally{setBusy(false)}};return <><div className="top"><div><div className="eyebrow">Payments / Receipts</div><div className="title">Combined receipt</div><div className="muted">One auditable view of gaming, food, credits, deposits and session payments.</div></div>{receipt&&<button className="btn" onClick={()=>window.print()}>Print</button>}</div>{error&&<div className="loginError" role="alert">{error}</div>}<section className="section card"><div className="sectionHead"><div className="sectionTitle">Find session receipt</div><span className="muted">Staff only</span></div><div style={{display:'flex',gap:10}}><input style={{flex:1}} placeholder="Session ID" value={id} onChange={e=>setId(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')load()}}/><button className="btn primary" disabled={busy} onClick={load}>{busy?'Loading…':'Open receipt'}</button></div></section>{receipt&&<section className="section card" id="receipt"><div className="sectionHead"><div><div className="eyebrow">{receipt.receiptId}</div><div className="sectionTitle">GenZ Gaming Cafe</div><div className="muted">{receipt.customerName} · {receipt.stationId} · Session {receipt.sessionId}</div></div><span className="badge green">RECEIPT</span></div><div className="grid4"><div><div className="statLabel">Gaming</div><b>{money(receipt.totals.gaming)}</b></div><div><div className="statLabel">Food</div><b>{money(receipt.totals.food)}</b></div><div><div className="statLabel">Paid</div><b>{money(receipt.totals.paid)}</b></div><div><div className="statLabel">Outstanding</div><b>{money(receipt.totals.outstanding)}</b></div></div><div className="sectionTitle" style={{marginTop:18}}>Gaming</div><div className="tableWrap"><table className="table"><thead><tr><th>Participant</th><th>Tier</th><th>Charge</th></tr></thead><tbody>{receipt.gaming.participants.map((p,i)=><tr key={i}><td>{p.name}</td><td>{p.memberTier||'Regular'}</td><td>{money(p.charge)}</td></tr>)}{!receipt.gaming.participants.length&&<tr><td colSpan={3} className="muted">Session-level gaming charge</td></tr>}</tbody></table></div><div className="sectionTitle" style={{marginTop:18}}>Food</div><div className="tableWrap"><table className="table"><thead><tr><th>Order</th><th>Items</th><th>Payment</th><th>Total</th></tr></thead><tbody>{receipt.food.map(o=><tr key={o.orderId}><td>{o.orderId}<br/><span className="muted">{o.status}</span></td><td>{o.items.map((x,i)=><div key={i}>{x.qty} × {x.name} {x.participantName?`· ${x.participantName}`:''}</div>)}</td><td>{o.paymentStatus}<br/><span className="muted">{o.paymentMode}</span></td><td>{money(o.total)}</td></tr>)}{!receipt.food.length&&<tr><td colSpan={4} className="muted">No food orders.</td></tr>}</tbody></table></div><div className="sectionTitle" style={{marginTop:18}}>Adjustments & payments</div><div className="tableWrap"><table className="table"><tbody><tr><td>Deposit applied</td><td>{money(receipt.totals.depositApplied)}</td></tr><tr><td>Group gaming allocation</td><td>{money(receipt.totals.groupAllocatedGaming)}</td></tr><tr><td>Group food allocation</td><td>{money(receipt.totals.groupAllocatedFood)}</td></tr><tr><td>Billing adjustments</td><td>{money(receipt.totals.adjustments)}</td></tr><tr><td>Monthly credit</td><td>{money(receipt.totals.creditApplied)}</td></tr></tbody></table></div><div className="sectionTitle" style={{marginTop:18}}>Session payments</div><div className="tableWrap"><table className="table"><thead><tr><th>Time</th><th>Method</th><th>Status</th><th>Amount</th></tr></thead><tbody>{receipt.payments.map(p=><tr key={p.id}><td>{new Date(p.createdAt).toLocaleString('en-IN')}</td><td>{p.method}</td><td>{p.status}{p.voidReason?` · ${p.voidReason}`:''}</td><td>{money(p.amount)}</td></tr>)}{!receipt.payments.length&&<tr><td colSpan={4} className="muted">No session payments.</td></tr>}</tbody></table></div></section>}</>}
