import {NextResponse} from 'next/server';
import {cookies} from 'next/headers';
import {COOKIE,requireStaff,createStaff,updateStaff,listStaff,audit} from '../../../../lib/staff-auth';
const auth=async()=>requireStaff((await cookies()).get(COOKIE)?.value);
export async function GET(){try{const staff=await auth();if(staff.role!=='OWNER')throw Error('STAFF_FORBIDDEN');return NextResponse.json({ok:true,staff:await listStaff()})}catch(e){const m=e instanceof Error?e.message:'Staff request failed';return NextResponse.json({ok:false,error:m},{status:m==='STAFF_FORBIDDEN'?403:401})}}
export async function POST(req:Request){try{const staff=await auth();if(staff.role!=='OWNER')throw Error('STAFF_FORBIDDEN');const b=await req.json();if(b.action==='create'){const result=await createStaff({username:String(b.username||''),name:String(b.name||''),role:b.role,password:String(b.password||'')});await audit(staff.id,'STAFF_CREATED','staff_user',result.id,{username:result.username,role:result.role});return NextResponse.json({ok:true,staff:result})}if(b.action==='update'){const id=String(b.id||'');const result=await updateStaff(id,{name:typeof b.name==='string'?b.name:undefined,role:b.role,active:typeof b.active==='boolean'?b.active:undefined,password:typeof b.password==='string'?b.password:undefined});await audit(staff.id,'STAFF_UPDATED','staff_user',id,{changed:true});return NextResponse.json({ok:true,updated:result})}throw Error('INVALID_STAFF_ACTION')}catch(e){const m=e instanceof Error?e.message:'Staff update failed';return NextResponse.json({ok:false,error:m},{status:m==='STAFF_FORBIDDEN'?403:m==='STAFF_UNAUTHORIZED'?401:400})}}
