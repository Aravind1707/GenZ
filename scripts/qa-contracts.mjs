import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const read=f=>fs.readFileSync(path.join(root,f),'utf8');const exists=f=>fs.existsSync(path.join(root,f));const failures=[];const check=(n,c)=>{if(!c)failures.push(n)};const has=(f,p)=>p.test(read(f));
check('package has production build and test scripts',has('package.json',/"build"\s*:\s*"next build"/)&&has('package.json',/"test"\s*:\s*"npm run test:integrity && npm run test:qa && npm run test:unit"/));check('package has unit/integration/coverage scripts',has('package.json',/"test:unit"/)&&has('package.json',/"test:integration"/)&&has('package.json',/"test:coverage"/));check('CI has required jobs',has('.github/workflows/ci.yml',/unit-build:/)&&has('.github/workflows/ci.yml',/mysql-integration:/)&&has('.github/workflows/ci.yml',/docker:/)&&has('.github/workflows/ci.yml',/security:/));
const migrations=fs.readdirSync(path.join(root,'db','migrations')).filter(f=>/^\d+_.+\.sql$/.test(f));const versions=migrations.map(f=>Number(f.split('_',1)[0]));check('migrations have no duplicates',new Set(versions).size===versions.length);check('latest migration is 048',Math.max(...versions)===48);
check('food pricing and availability are server authoritative',has('lib/pricing.ts',/member_price/)&&has('lib/pricing.ts',/available/)&&has('lib/pricing.ts',/inventory_batches/)&&has('app/api/orders/route.ts',/createFoodOrder/));check('customer unavailable overlay and responsive UI exists',has('app/customer/FoodCart.tsx',/NOT AVAILABLE RIGHT NOW/)&&has('app/customer/FoodCart.tsx',/disabled=\{!i\.available\}/));check('server validates unavailable food through atomic reservation',has('lib/food-orders.ts',/reserveOrderStock/)&&has('lib/inventory.ts',/OUT_OF_STOCK/)&&has('lib/inventory.ts',/FOR UPDATE/));check('FIFO delivery consumption and COGS exists',has('lib/food-orders.ts',/deliverFoodOrder/)&&has('lib/inventory.ts',/consumeMaterialFifo/)&&has('lib/inventory.ts',/inventory_cogs_ledger/)&&has('lib/inventory.ts',/expiry_at IS NULL OR b\.expiry_at>=CURDATE/));check('order cancellation releases reservations',has('lib/food-orders.ts',/cancelFoodOrder/)&&has('lib/food-orders.ts',/releaseOrderStock/));check('food cancellation is limited to NEW and ACCEPTED before PREPARING',has('lib/food-orders.ts',/!\['NEW','ACCEPTED'\]\.includes\(String\(r\[0\]\.status\)\)/)&&has('lib/food-orders.ts',/Order can only be cancelled before preparation starts/));check('food refund policy is 100 percent before preparation and 50 percent after',has('lib/food-orders.ts',/policyPercent:50\|100/)&&has('lib/food-orders.ts',/status==='NEW'\|\|status==='ACCEPTED'\?100:50/)&&has('lib/food-orders.ts',/ADMIN_DESK_CASH/)&&has('db/migrations/048_food_refund_policy.sql',/food_order_refunds/));check('customer food cancellation endpoint exists',has('app/api/customer/orders/route.ts',/cancelFoodOrderByCustomer/)&&has('app/api/customer/orders/route.ts',/ADMIN_DESK_CASH/));check('admin desk food refund action exists',has('app/api/orders/route.ts',/refund-food/)&&has('lib/food-orders.ts',/refundFoodOrder/));check('idempotent ordering prevents duplicate checkout',has('lib/food-orders.ts',/client_idempotency_key/)&&has('lib/food-orders.ts',/IDEMPOTENCY_CONFLICT/));check('inventory negative stock prevented',has('lib/inventory.ts',/INSUFFICIENT_BATCH_STOCK/)&&has('lib/inventory.ts',/reserved>=/));check('inventory unit/integration suite exists',exists('tests/unit/inventory.test.ts')&&exists('scripts/qa-mysql.mjs'));
check('provider reconciliation exists',exists('lib/payment-provider-reconciliation.ts')&&exists('app/api/finance/provider-reconciliation/route.ts'));check('daily close controls exist',has('lib/daily-close-report.ts',/approveDailyClose/)&&has('lib/daily-close-report.ts',/reopenDailyClose/));check('station agent fail-safe exists',has('lib/station-agent-protocol.ts',/OFFLINE/)&&has('lib/station-agent-protocol.ts',/LOCKED/)&&has('lib/station-agent-protocol.ts',/ERROR/));check('request ID and realtime replay exist',has('middleware.ts',/x-request-id/)&&exists('lib/realtime.ts')&&exists('app/api/events/route.ts'));check('migration validator exists',exists('scripts/validate-migrations.mjs'));
if(failures.length){console.error(`QA contract checks failed (${failures.length}):`);for(const f of failures)console.error(`- ${f}`);process.exit(1)}console.log(`QA contract checks passed: ${migrations.length} migrations and food/inventory/payment controls.`);
